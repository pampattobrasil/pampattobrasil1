const DEFAULT_PRODUCTS=[{"nome": "Picanha", "tipo": "Carnes Bovinas", "imagem": "assets/images/picanha.jpg"}, {"nome": "Contrafilé", "tipo": "Carnes Bovinas", "imagem": "assets/images/contrafile.jpg"}, {"nome": "Costela bovina", "tipo": "Carnes Bovinas", "imagem": "assets/images/costela-bovina.jpg"}, {"nome": "Carne moída", "tipo": "Carnes Bovinas", "imagem": "assets/images/carne-moida.jpg"}, {"nome": "Cupim", "tipo": "Carnes Bovinas", "imagem": "assets/images/cupim.jpg"}, {"nome": "Patinho em cubos", "tipo": "Carnes Bovinas", "imagem": "assets/images/patinho-em-cubos.jpg"}, {"nome": "Acém", "tipo": "Carnes Bovinas", "imagem": "assets/images/acem.jpg"}, {"nome": "Coxão mole", "tipo": "Carnes Bovinas", "imagem": "assets/images/coxao-mole.jpg"}, {"nome": "Coxão duro", "tipo": "Carnes Bovinas", "imagem": "assets/images/coxao-duro.jpg"}, {"nome": "Lagarto", "tipo": "Carnes Bovinas", "imagem": "assets/images/lagarto.jpg"}, {"nome": "Costela suína", "tipo": "Carnes Suínas", "imagem": "assets/images/costela-suina.jpg"}, {"nome": "Pernil em cubos", "tipo": "Carnes Suínas", "imagem": "assets/images/pernil-em-cubos.jpg"}, {"nome": "Bacon", "tipo": "Carnes Suínas", "imagem": "assets/images/bacon.jpg"}, {"nome": "Frango inteiro", "tipo": "Carnes de Frango", "imagem": "assets/images/frango-inteiro.jpg"}, {"nome": "Peito de frango", "tipo": "Carnes de Frango", "imagem": "assets/images/peito-de-frango.jpg"}, {"nome": "Filé de peito", "tipo": "Carnes de Frango", "imagem": "assets/images/file-de-peito.jpg"}, {"nome": "Coxa e sobrecoxa", "tipo": "Carnes de Frango", "imagem": "assets/images/coxa-e-sobrecoxa.jpg"}, {"nome": "Meio da asa", "tipo": "Carnes de Frango", "imagem": "assets/images/meio-da-asa.jpg"}, {"nome": "Linguiça de frango", "tipo": "Carnes de Frango", "imagem": "assets/images/linguica-de-frango.jpg"}, {"nome": "Hambúrguer de frango", "tipo": "Carnes de Frango", "imagem": "assets/images/hamburguer-de-frango.jpg"}, {"nome": "Almôndegas", "tipo": "Carnes de Frango", "imagem": "assets/images/almondegas.jpg"}, {"nome": "Moela", "tipo": "Miúdos de Frango", "imagem": "assets/images/moela.jpg"}, {"nome": "Coração", "tipo": "Miúdos de Frango", "imagem": "assets/images/coracao.jpg"}, {"nome": "Fígado", "tipo": "Miúdos de Frango", "imagem": "assets/images/figado.jpg"}, {"nome": "Pé de frango", "tipo": "Miúdos de Frango", "imagem": "assets/images/pe-de-frango.jpg"}, {"nome": "Linguiça toscana", "tipo": "Embutidos", "imagem": "assets/images/linguica-toscana.jpg"}, {"nome": "Hambúrguer", "tipo": "Industrializados", "imagem": "assets/images/hamburguer.jpg"}, {"nome": "Almôndegas", "tipo": "Industrializados", "imagem": "assets/images/almondegas-29.jpg"}, {"nome": "Tilápia", "tipo": "Peixes", "imagem": "assets/images/tilapia.jpg"}, {"nome": "Salmão", "tipo": "Peixes", "imagem": "assets/images/salmao.jpg"}, {"nome": "Filé de peixe panga", "tipo": "Peixes", "imagem": "assets/images/file-de-peixe-panga.jpg"}];

const $=id=>document.getElementById(id);
const STORAGE='pampattoStateV4';
const legacy=localStorage.getItem('pampattoStateV3');
let filtroTipo='Todos',currentUser=null;
const initialProducts=()=>DEFAULT_PRODUCTS.map((p,i)=>({id:String(i+1),nome:p.nome,tipo:p.tipo,imagem:p.imagem,fabricante:'Empório Pampatto',quantidade:0,validade:'',valor:0}));
let state;
try{state=JSON.parse(localStorage.getItem(STORAGE)||legacy||'null')}catch(e){state=null}
if(!state)state={produtos:initialProducts(),pedidos:[],carrinhos:{},usuarios:[{id:'admin',nome:'Administrador',usuario:'teste',senha:'teste',perfil:'admin',ativo:true}]};
if(!Array.isArray(state.produtos))state.produtos=initialProducts();
state.produtos.forEach(p=>{if(p.valor==null)p.valor=0});
if(!Array.isArray(state.pedidos))state.pedidos=[];
if(!state.carrinhos)state.carrinhos={};
if(!Array.isArray(state.usuarios))state.usuarios=[];
if(!state.usuarios.some(u=>u.perfil==='admin'))state.usuarios.unshift({id:'admin',nome:'Administrador',usuario:'teste',senha:'teste',perfil:'admin',ativo:true});
function save(){localStorage.setItem(STORAGE,JSON.stringify(state))}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}
function qtd(p){return Number(p.quantidade||0)}
function isAdmin(){return currentUser&&currentUser.perfil==='admin'}
function cart(){if(!currentUser)return[];state.carrinhos[currentUser.id]??=[];return state.carrinhos[currentUser.id]}
const categoryIcons={'Carnes Bovinas':'♉','Carnes Suínas':'♘','Carnes de Frango':'♞','Miúdos de Frango':'♞','Embutidos':'▣','Industrializados':'▤','Peixes':'♓'};
function catButtons(){const cats=['Todos','Carnes Bovinas','Carnes Suínas','Carnes de Frango','Miúdos de Frango','Embutidos','Industrializados','Peixes'];return `<div class="category-buttons">${cats.map(c=>`<button class="cat-btn ${filtroTipo===c?'active':''}" data-cat="${c}">${c}</button>`).join('')}</div>`}
function card(p){return `<article class="product-card"><img src="${p.imagem}" alt="${esc(p.nome)}"><div class="product-meta"><h4 class="product-name">${esc(p.nome)}</h4><div class="price">${money(p.valor)}</div></div><div class="buy-box"><div class="qty-control"><button type="button" aria-label="Diminuir quantidade" onclick="alterarQtdCompra('${p.id}',-1)">−</button><input id="buyQty-${p.id}" type="number" min="1" max="999" value="1" inputmode="numeric" aria-label="Quantidade de ${esc(p.nome)}"><button type="button" aria-label="Aumentar quantidade" onclick="alterarQtdCompra('${p.id}',1)">+</button></div><button class="cart-add" type="button" onclick="enviarCarrinho('${p.id}')">Incluir</button></div></article>`}
function renderProductArea(targetId){const input=targetId==='dashProducts'?$('buscaDash'):$('buscaProdutos');const termo=(input?.value||'').toLowerCase();const base=state.produtos.filter(p=>(filtroTipo==='Todos'||p.tipo===filtroTipo)).filter(p=>`${p.nome} ${p.tipo}`.toLowerCase().includes(termo));let html=catButtons();const cats=filtroTipo==='Todos'?Object.keys(categoryIcons):[filtroTipo];cats.forEach(cat=>{const items=base.filter(p=>p.tipo===cat);if(items.length)html+=`<section class="product-section"><div class="section-title"><span>${categoryIcons[cat]||'•'}</span>${cat}</div><div class="product-grid">${items.map(card).join('')}</div></section>`});$(targetId).innerHTML=html||'<div class="empty">Nenhum produto encontrado.</div>';$(targetId).querySelectorAll('.cat-btn').forEach(b=>b.onclick=()=>{filtroTipo=b.dataset.cat;renderAll()})}
function renderTabela(){if(!isAdmin())return;const termo=($('busca').value||'').toLowerCase();const lista=state.produtos.filter(p=>`${p.nome} ${p.tipo}`.toLowerCase().includes(termo));$('tbody').innerHTML=lista.map(p=>`<tr><td><img class="thumb" src="${p.imagem}" alt="Produto"></td><td><strong>${esc(p.nome)}</strong><br><span class="muted">${esc(p.fabricante)}</span></td><td><span class="tag">${esc(p.tipo)}</span></td><td><input class="stock-edit" id="stock-price-${p.id}" type="number" min="0" step="0.01" value="${Number(p.valor||0).toFixed(2)}"></td><td><input class="stock-edit" id="stock-qty-${p.id}" type="number" min="0" step="0.01" value="${qtd(p)}"></td><td>${qtd(p)<=0?'<span class="low">Esgotado</span>':'Disponível'}</td><td><div class="actions"><button class="mini-btn" onclick="salvarEstoqueLinha('${p.id}')">Salvar quantidade e valor</button><button class="mini-btn" onclick="editProduto('${p.id}')">Editar dados</button></div><span class="stock-save-ok" id="stock-ok-${p.id}"></span></td></tr>`).join('')||'<tr><td colspan="7" class="empty">Nenhum produto encontrado.</td></tr>'}
window.salvarEstoqueLinha=id=>{const p=state.produtos.find(x=>x.id===id);const q=Number(String($('stock-qty-'+id)?.value??'').replace(',','.'));const v=Number(String($('stock-price-'+id)?.value??'').replace(',','.'));if(!p||isNaN(q)||q<0)return alert('Informe uma quantidade válida.');if(isNaN(v)||v<0)return alert('Informe um valor válido.');p.quantidade=q;p.valor=v;save();renderAll();const aviso=$('stock-ok-'+id);if(aviso){aviso.textContent='Atualizado com sucesso';setTimeout(()=>{if(aviso)aviso.textContent=''},2200)}};
function getCartQty(id){return cart().filter(i=>i.produtoId===id).reduce((s,i)=>s+Number(i.quantidade||0),0)}
function setCartNotice(msg,type='success'){const n=$('cartNotice');if(!n)return;n.className=type==='error'?'cart-warning':'cart-success';n.textContent=msg;setTimeout(()=>{if(n.textContent===msg)n.textContent=''},3500)}
window.alterarQtdCompra=(id,delta)=>{const input=$('buyQty-'+id);if(!input)return;input.value=Math.max(1,Math.min(999,Number(input.value||1)+delta))};
window.enviarCarrinho=id=>{const p=state.produtos.find(x=>x.id===id),input=$('buyQty-'+id);if(!p||!input)return;const quantidade=Math.max(1,Math.min(999,Number(input.value||1)));const item=cart().find(i=>i.produtoId===id);if(item)item.quantidade=Math.min(999,Number(item.quantidade||0)+quantidade);else cart().push({produtoId:id,quantidade});save();renderAll();setCartNotice('Produto incluído no carrinho.');input.value=1};
window.alterarQtdCarrinho=(id,delta)=>{const item=cart().find(i=>i.produtoId===id);if(!item)return;item.quantidade=Math.max(1,Math.min(999,Number(item.quantidade)+delta));save();renderAll()};
window.removerCarrinho=id=>{state.carrinhos[currentUser.id]=cart().filter(i=>i.produtoId!==id);save();renderAll()};
function renderCarrinho(){const c=cart(),badge=$('cartBadge'),view=$('cartView'),totalItens=c.reduce((s,i)=>s+Number(i.quantidade),0);badge.textContent=totalItens;badge.style.display=totalItens?'grid':'none';if(!c.length){view.innerHTML='<div class="empty">Seu carrinho está vazio.</div>';return}let total=0;view.innerHTML=`<div class="cart-list">${c.map(i=>{const p=state.produtos.find(x=>x.id===i.produtoId);if(!p)return'';const subtotal=Number(p.valor||0)*Number(i.quantidade);total+=subtotal;return `<div class="cart-item"><img src="${p.imagem}" alt="Produto"><div><strong>${esc(p.nome)}</strong><br><span class="muted">${money(p.valor)} por unidade</span></div><div class="cart-item-price"><strong>${money(subtotal)}</strong><div class="cart-actions"><button onclick="alterarQtdCarrinho('${p.id}',-1)">−</button><strong>${i.quantidade}</strong><button onclick="alterarQtdCarrinho('${p.id}',1)">+</button><button onclick="removerCarrinho('${p.id}')">×</button></div></div></div>`}).join('')}</div><div class="cart-total"><span>Valor total do pedido</span><strong>${money(total)}</strong></div>`}
function gerarNumeroPedido(){const agora=new Date();const dd=String(agora.getDate()).padStart(2,'0');const mm=String(agora.getMonth()+1).padStart(2,'0');const aa=String(agora.getFullYear()).slice(-2);const maiorSequencia=state.pedidos.reduce((maior,pedido)=>{const numero=String(pedido.numero||'');const match=numero.match(/(\d{4,})$/);if(!match)return maior;const sequencia=Number(match[1]);return Number.isFinite(sequencia)&&sequencia>=3000?Math.max(maior,sequencia):maior},2999);return `${dd}${mm}${aa}${maiorSequencia+1}`}
function finalizarCompra(){const c=cart();if(!c.length)return setCartNotice('Seu carrinho está vazio.','error');const itens=c.map(i=>{const p=state.produtos.find(x=>x.id===i.produtoId);return{produtoId:p.id,nome:p.nome,quantidade:Number(i.quantidade),valorUnitario:Number(p.valor||0),subtotal:Number(i.quantidade)*Number(p.valor||0)}});const pedido={id:crypto.randomUUID(),numero:gerarNumeroPedido(),data:new Date().toISOString(),clienteId:currentUser.id,clienteNome:currentUser.nome,clienteUsuario:currentUser.usuario,status:'Pedido realizado',itens,total:itens.reduce((s,i)=>s+i.subtotal,0)};state.pedidos.push(pedido);state.carrinhos[currentUser.id]=[];save();renderAll();setCartNotice(`Pedido nº ${pedido.numero} realizado com sucesso.`)}
function statusOptions(p){const etapas=['Pedido realizado','Em separação','Separado','Entregue'];if(!isAdmin())return `<span class="role-badge">${esc(p.status||'Pedido realizado')}</span>`;return `<select class="order-status-select" onchange="alterarStatusPedido('${p.id}',this.value)">${etapas.map(et=>`<option value="${et}" ${(p.status||'Pedido realizado')===et?'selected':''}>${et}</option>`).join('')}</select>`}
window.alterarStatusPedido=(id,status)=>{if(!isAdmin())return;const pedido=state.pedidos.find(p=>p.id===id);if(!pedido)return;pedido.status=status;save();renderPedidos();renderMetrics()};
function orderHtml(p){const etapas=['Pedido realizado','Em separação','Separado','Entregue'];const atual=Math.max(0,etapas.indexOf(p.status||'Pedido realizado'));return `<div class="order-card"><div class="order-head"><div><strong>Pedido nº ${esc(p.numero||p.id.slice(0,6))}</strong><br><span class="muted">${new Date(p.data).toLocaleString('pt-BR')} · ${esc(p.clienteNome||'Cliente')}</span></div><div>${statusOptions(p)}<br><strong>${money(p.total)}</strong></div></div><div class="order-track">${etapas.map((et,i)=>`<div class="order-step ${i<=atual?'done':''}"><span>${i+1}</span><small>${et}</small></div>`).join('')}</div><div class="order-items">${(p.itens||[]).map(i=>`<div class="order-line"><span>${esc(i.nome||state.produtos.find(x=>x.id===i.produtoId)?.nome||'Produto')} × ${i.quantidade}</span><strong>${money(i.subtotal??(Number(i.valorUnitario||0)*Number(i.quantidade||0)))}</strong></div>`).join('')}</div></div>`}
function currentMonth(){return new Date().toISOString().slice(0,7)}
function renderPedidos(){
  const box=$('ordersContent');
  if(!currentUser||!box)return;
  const list=(isAdmin()?state.pedidos:state.pedidos.filter(p=>p.clienteId===currentUser.id)).sort((a,b)=>new Date(b.data)-new Date(a.data));
  box.innerHTML=`<div class="panel"><div class="panel-head"><div><h3>${isAdmin()?'Pedidos realizados':'Meus pedidos'}</h3><p class="muted">${isAdmin()?'Consulte todos os pedidos registrados pelos clientes.':'Histórico dos pedidos realizados.'}</p></div></div><div>${list.map(orderHtml).join('')||'<div class="empty">Nenhum pedido realizado.</div>'}</div></div>`;
}
function renderRelatorios(){
  if(!isAdmin())return;
  const box=$('reportsContent');
  if(!box)return;
  const month=$('reportMonth')?.value||currentMonth();
  const cliente=$('reportClient')?.value||'Todos';
  const list=state.pedidos.filter(p=>p.data.slice(0,7)===month&&(cliente==='Todos'||p.clienteId===cliente)).sort((a,b)=>new Date(b.data)-new Date(a.data));
  box.innerHTML=`<div class="panel"><div class="panel-head"><div><h3>Relatórios mensais</h3><p class="muted">Filtre as compras por mês e cliente e gere o relatório em PDF.</p></div></div><div class="report-filter"><div><label>Mês</label><input type="month" id="reportMonth" value="${month}"></div><div><label>Cliente</label><select id="reportClient"><option value="Todos">Todos</option>${state.usuarios.filter(u=>u.perfil==='cliente').map(u=>`<option value="${u.id}" ${cliente===u.id?'selected':''}>${esc(u.nome)}</option>`).join('')}</select></div><button class="outline-btn" id="filterReportBtn">Filtrar</button><button class="btn" id="pdfReportBtn">Gerar PDF</button></div><div class="cart-total"><span>Total do período (${list.length} pedido(s))</span><strong>${money(list.reduce((s,p)=>s+Number(p.total||0),0))}</strong></div><div style="margin-top:16px">${list.map(orderHtml).join('')||'<div class="empty">Nenhum pedido no período.</div>'}</div></div>`;
  $('filterReportBtn').onclick=renderRelatorios;
  $('pdfReportBtn').onclick=()=>gerarRelatorioPDF(month,cliente);
}
window.salvarPreco=id=>{const p=state.produtos.find(x=>x.id===id),v=Number($('price-'+id).value);if(!p||isNaN(v)||v<0)return alert('Valor inválido.');p.valor=v;save();renderAll();alert('Valor atualizado.')};
function gerarRelatorioPDF(month,cliente){const list=state.pedidos.filter(p=>p.data.slice(0,7)===month&&(cliente==='Todos'||p.clienteId===cliente));const total=list.reduce((s,p)=>s+Number(p.total||0),0);const rows=list.flatMap(p=>(p.itens||[]).map(i=>`<tr><td>${new Date(p.data).toLocaleDateString('pt-BR')}</td><td>${esc(p.numero)}</td><td>${esc(p.clienteNome)}</td><td>${esc(i.nome)}</td><td>${i.quantidade}</td><td>${money(i.valorUnitario)}</td><td>${money(i.subtotal)}</td></tr>`)).join('');const w=window.open('','_blank');w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Relatório ${month}</title></head><body><h1>Empório Pampatto Brasil</h1><p>Relatório mensal de compras — ${month.split('-').reverse().join('/')}</p><p>Gerado em ${new Date().toLocaleString('pt-BR')}</p><table><thead><tr><th>Data</th><th>Pedido</th><th>Cliente</th><th>Produto</th><th>Qtd.</th><th>Valor unit.</th><th>Subtotal</th></tr></thead><tbody>${rows||'<tr><td colspan="7">Nenhum pedido no período.</td></tr>'}</tbody></table><div class="total"><strong>Total geral: ${money(total)}</strong></div><button onclick="window.print()">Imprimir / Salvar em PDF</button><script>setTimeout(()=>window.print(),400)<\/script></body></html>`);w.document.close()}
function renderUsers(){if(!isAdmin())return;$('usersBody').innerHTML=state.usuarios.map(u=>`<tr><td>${esc(u.nome)}</td><td>${esc(u.usuario)}</td><td><span class="role-badge">${u.perfil==='admin'?'Administrador':'Cliente'}</span></td><td>${u.ativo!==false?'Ativo':'Inativo'}</td><td>${u.perfil==='admin'?'<span class="muted">Acesso principal</span>':`<button class="mini-btn" onclick="toggleUser('${u.id}')">${u.ativo!==false?'Desativar':'Ativar'}</button><button class="mini-btn danger" onclick="deleteUser('${u.id}')">Excluir</button>`}</td></tr>`).join('')}
window.toggleUser=id=>{const u=state.usuarios.find(x=>x.id===id);u.ativo=u.ativo===false;save();renderUsers()};window.deleteUser=id=>{if(!confirm('Excluir este cliente? O histórico de pedidos será mantido.'))return;state.usuarios=state.usuarios.filter(u=>u.id!==id);save();renderUsers()};
function renderMetrics(){const own=isAdmin()?state.pedidos:state.pedidos.filter(p=>p.clienteId===currentUser.id),month=currentMonth(),monthly=state.pedidos.filter(p=>p.data.slice(0,7)===month);$('metricProducts').textContent=state.produtos.length;$('metricOrders').textContent=own.length;if(isAdmin()){$('metricLow').textContent=state.produtos.filter(p=>qtd(p)<=0).length;$('metricSales').textContent=money(monthly.reduce((s,p)=>s+Number(p.total||0),0))}}
function applyPermissions(){document.querySelectorAll('.admin-only').forEach(el=>el.style.display=isAdmin()?'':'none');document.querySelectorAll('.client-only').forEach(el=>el.style.display='');$('currentUserName').textContent=currentUser.nome;$('currentUserRole').textContent=isAdmin()?'Administrador':currentUser.usuario;$('ordersMenuLabel').textContent=isAdmin()?'Pedidos':'Meus pedidos'}
function renderAll(){if(!currentUser)return;applyPermissions();renderProductArea('dashProducts');renderProductArea('produtosView');if(isAdmin()){renderTabela();renderUsers()}renderCarrinho();renderPedidos();if(isAdmin())renderRelatorios();renderMetrics()}
function openTab(tab){if((tab==='estoque'||tab==='empresa'||tab==='relatorios')&&!isAdmin())return;document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));$('tab-'+tab)?.classList.add('active');document.querySelectorAll('.nav button').forEach(b=>b.classList.remove('active'));document.querySelector(`[data-tab="${tab}"]`)?.classList.add('active');if(tab==='pedidos')renderPedidos();if(tab==='relatorios')renderRelatorios()}
document.querySelectorAll('.nav button[data-tab]').forEach(btn=>btn.onclick=()=>openTab(btn.dataset.tab));document.querySelectorAll('[data-new-product]').forEach(b=>b.onclick=()=>openTab('estoque'));
$('loginForm').onsubmit=e=>{e.preventDefault();const user=state.usuarios.find(u=>u.usuario===$('login').value.trim()&&u.senha===$('senha').value&&u.ativo!==false);if(!user){$('errorAlert').style.display='block';return}currentUser=user;$('errorAlert').style.display='none';$('loginPage').style.display='none';$('appPage').style.display='block';openTab('dashboard');renderAll()};
$('toggleSenha').onclick=()=>{$('senha').type=$('senha').type==='password'?'text':'password'};$('senha').addEventListener('keyup',e=>{$('capsAlert').style.display=e.getModifierState('CapsLock')?'block':'none'});$('logoutBtn').onclick=()=>{currentUser=null;$('appPage').style.display='none';$('loginPage').style.display='flex';$('senha').value=''};
window.ajustarEstoque=id=>{const p=state.produtos.find(x=>x.id===id),n=prompt('Informe a nova quantidade:',qtd(p));if(n===null)return;const v=Number(String(n).replace(',','.'));if(isNaN(v)||v<0)return alert('Quantidade inválida.');p.quantidade=v;save();renderAll()};
window.editProduto=id=>{const p=state.produtos.find(x=>x.id===id);$('produtoId').value=p.id;$('nome').value=p.nome;$('fabricante').value=p.fabricante;$('quantidade').value=qtd(p);$('valor').value=Number(p.valor||0).toFixed(2);$('tipo').value=p.tipo;$('validade').value=p.validade||'';$('stockFormTitle').textContent='Editar produto';$('cancelProductEdit').style.display='block';openTab('estoque')};
$('stockForm').onsubmit=e=>{e.preventDefault();const id=$('produtoId').value,data={nome:$('nome').value.trim(),fabricante:$('fabricante').value.trim()||'Empório Pampatto',quantidade:Number($('quantidade').value||0),valor:Number($('valor').value||0),tipo:$('tipo').value,validade:$('validade').value};if(!data.nome)return alert('Informe o nome interno do produto.');const done=img=>{if(id){const p=state.produtos.find(x=>x.id===id);Object.assign(p,data);if(img)p.imagem=img}else state.produtos.unshift({id:crypto.randomUUID(),...data,imagem:img||DEFAULT_PRODUCTS[0].imagem});save();e.target.reset();$('produtoId').value='';$('stockFormTitle').textContent='Cadastrar produto';$('cancelProductEdit').style.display='none';renderAll()};const file=$('imagem').files[0];if(file){const r=new FileReader();r.onload=ev=>done(ev.target.result);r.readAsDataURL(file)}else done(null)};
$('cancelProductEdit').onclick=()=>{$('stockForm').reset();$('produtoId').value='';$('stockFormTitle').textContent='Cadastrar produto';$('cancelProductEdit').style.display='none'};
$('userForm').onsubmit=e=>{e.preventDefault();const usuario=$('clienteUsuario').value.trim();if(state.usuarios.some(u=>u.usuario.toLowerCase()===usuario.toLowerCase()))return alert('Este usuário já existe.');state.usuarios.push({id:crypto.randomUUID(),nome:$('clienteNome').value.trim(),usuario,senha:$('clienteSenha').value,perfil:'cliente',ativo:true});save();e.target.reset();renderUsers();const n=$('userNotice');n.style.display='block';n.textContent='Cliente cadastrado com sucesso.';setTimeout(()=>n.style.display='none',3000)};
$('busca').oninput=renderTabela;$('buscaDash').oninput=()=>renderProductArea('dashProducts');$('buscaProdutos').oninput=()=>renderProductArea('produtosView');$('clearCartBtn').onclick=()=>{state.carrinhos[currentUser.id]=[];save();renderAll();setCartNotice('Carrinho limpo.')};$('finishCartBtn').onclick=finalizarCompra;
$('exportBtn').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'}));a.download='emporio-pampatto-backup.json';a.click();URL.revokeObjectURL(a.href)};$('importBtn').onclick=()=>$('importFile').click();$('importFile').onchange=e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=ev=>{try{const imported=JSON.parse(ev.target.result);if(!imported.produtos||!imported.usuarios)throw Error();state=imported;save();renderAll();alert('Backup importado.')}catch{alert('Arquivo de backup inválido.')}};r.readAsText(f)};
save();
